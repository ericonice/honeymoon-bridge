{
  "name": "@hb/server",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker and Durable Object: the authority for a networked table.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hb/engine": "*",
    "@hb/protocol": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5",
    "wrangler": "^4.120.0"
  }
}
